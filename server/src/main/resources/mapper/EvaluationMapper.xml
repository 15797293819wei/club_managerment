<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.club.mapper.EvaluationMapper">

  <resultMap id="EvaluationMap" type="com.example.club.domain.Evaluation">
    <id column="id" property="id"/>
    <result column="activity_id" property="activityId"/>
    <result column="user_id" property="userId"/>
    <result column="rating" property="rating"/>
    <result column="comment" property="comment"/>
    <result column="created_time" property="createdTime"/>
    <result column="updated_time" property="updatedTime"/>
  </resultMap>

  <sql id="BaseColumns">
    id, activity_id, user_id, rating, comment, created_time, updated_time
  </sql>

  <select id="selectById" resultMap="EvaluationMap">
    select
    <include refid="BaseColumns"/>
    from evaluations
    where id = #{id}
  </select>

  <select id="selectByActivityIdAndUserId" resultMap="EvaluationMap">
    select
    <include refid="BaseColumns"/>
    from evaluations
    where activity_id = #{activityId} and user_id = #{userId}
  </select>

  <select id="countByFilters" resultType="long">
    select count(1)
    from evaluations
    <where>
      <if test="activityId != null">
        and activity_id = #{activityId}
      </if>
      <if test="userId != null">
        and user_id = #{userId}
      </if>
    </where>
  </select>

  <select id="selectPage" resultMap="EvaluationMap">
    select
    <include refid="BaseColumns"/>
    from evaluations
    <where>
      <if test="activityId != null">
        and activity_id = #{activityId}
      </if>
      <if test="userId != null">
        and user_id = #{userId}
      </if>
    </where>
    order by created_time desc
    limit #{offset}, #{size}
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    insert into evaluations (
      activity_id, user_id, rating, comment
    ) values (
      #{activityId}, #{userId}, #{rating}, #{comment}
    )
  </insert>

  <update id="updateById">
    update evaluations
    <set>
      <if test="rating != null">rating = #{rating},</if>
      <if test="comment != null">comment = #{comment},</if>
    </set>
    where id = #{id}
  </update>

  <delete id="deleteById">
    delete from evaluations
    where id = #{id}
  </delete>

  <select id="selectAverageRating" resultType="double">
    select ifnull(avg(rating), 0)
    from evaluations
    where activity_id = #{activityId}
  </select>

  <select id="countByActivityId" resultType="long">
    select count(1)
    from evaluations
    where activity_id = #{activityId}
  </select>

</mapper>

