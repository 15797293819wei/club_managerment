<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.club.mapper.MemberApplicationMapper">

  <resultMap id="MemberApplicationMap" type="com.example.club.domain.MemberApplication">
    <id column="id" property="id"/>
    <result column="club_id" property="clubId"/>
    <result column="applicant_id" property="applicantId"/>
    <result column="application_reason" property="applicationReason"/>
    <result column="status" property="status"/>
    <result column="reviewer_id" property="reviewerId"/>
    <result column="review_time" property="reviewTime"/>
    <result column="review_comment" property="reviewComment"/>
    <result column="created_time" property="createdTime"/>
    <result column="updated_time" property="updatedTime"/>
  </resultMap>

  <sql id="BaseColumns">
    id, club_id, applicant_id, application_reason, status, reviewer_id, review_time, review_comment, created_time, updated_time
  </sql>

  <select id="selectById" resultMap="MemberApplicationMap">
    select
    <include refid="BaseColumns"/>
    from member_applications
    where id = #{id}
  </select>

  <select id="selectPendingByClubIdAndApplicantId" resultMap="MemberApplicationMap">
    select
    <include refid="BaseColumns"/>
    from member_applications
    where club_id = #{clubId} and applicant_id = #{applicantId} and status = 0
    limit 1
  </select>

  <select id="countByFilters" resultType="long">
    select count(1)
    from member_applications
    <where>
      <if test="clubId != null">
        and club_id = #{clubId}
      </if>
      <if test="applicantId != null">
        and applicant_id = #{applicantId}
      </if>
      <if test="status != null">
        and status = #{status}
      </if>
    </where>
  </select>

  <select id="selectPage" resultMap="MemberApplicationMap">
    select
    <include refid="BaseColumns"/>
    from member_applications
    <where>
      <if test="clubId != null">
        and club_id = #{clubId}
      </if>
      <if test="applicantId != null">
        and applicant_id = #{applicantId}
      </if>
      <if test="status != null">
        and status = #{status}
      </if>
    </where>
    order by created_time desc
    limit #{size} offset #{offset}
  </select>

  <insert id="insert" parameterType="com.example.club.domain.MemberApplication" useGeneratedKeys="true" keyProperty="id">
    insert into member_applications (
      club_id, applicant_id, application_reason, status
    ) values (
      #{clubId}, #{applicantId}, #{applicationReason}, #{status}
    )
  </insert>

  <update id="updateById" parameterType="com.example.club.domain.MemberApplication">
    update member_applications
    <set>
      <if test="applicationReason != null">application_reason = #{applicationReason},</if>
      <if test="status != null">status = #{status},</if>
      <if test="reviewerId != null">reviewer_id = #{reviewerId},</if>
      <if test="reviewTime != null">review_time = #{reviewTime},</if>
      <if test="reviewComment != null">review_comment = #{reviewComment},</if>
    </set>
    where id = #{id}
  </update>

  <delete id="deleteById">
    delete from member_applications where id = #{id}
  </delete>

  <select id="selectRecentByApplicant" resultMap="MemberApplicationMap">
    select
    <include refid="BaseColumns"/>
    from member_applications
    where applicant_id = #{applicantId}
    order by created_time desc
    <if test="limit != null">
      limit #{limit}
    </if>
  </select>

</mapper>

